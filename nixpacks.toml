[phases.setup]
nixPkgs = ["python311", "nodejs_20"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "cd wallet-helper && npm ci"
]

[phases.build]
cmds = [
  "playwright install chromium --with-deps",
  "cd wallet-helper && npm run build"
]

[start]
cmd = "python agent.py"
