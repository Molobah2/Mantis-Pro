[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.build]
cmds = [
  "pip install -r requirements.txt",
  "playwright install chromium",
  "cd wallet-helper && npm ci && npm run build"
]

[start]
cmd = "python agent.py"
